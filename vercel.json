{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "if [ ! -d \"$HOME/flutter\" ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1 \"$HOME/flutter\"; fi; export PATH=\"$HOME/flutter/bin:$PATH\"; flutter --version; flutter pub get; flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}